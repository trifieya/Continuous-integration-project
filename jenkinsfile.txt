pipeline{
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages {
        stage('Getting project from Git') {
            steps{
                checkout([$class: 'GitSCM', branches: [[name: '*/factureT']], browser: [$class: 'https://github.com/trifieya/Continuous-integration-project.git'], extensions: [], userRemoteConfigs: [[ url: 'https://github.com/trifieya/Continuous-integration-project.git']]])
            }
        }
        
        
        stage('Cleaning the project') {
            steps{
                sh "mvn -B -DskipTests clean  " 
            }
        }
        
        
        stage('Artifact Construction') {
            steps{
                sh "mvn -B -DskipTests package " 
            }
        }
        
         stage('Unit Tests') {
            steps{
                sh "mvn test " 
            }
        }
        
        
        stage('Code Quality Check via SonarQube') {
            steps{
                
             sh "        mvn sonar:sonar   -Dsonar.projectKey=sonar   -Dsonar.host.url=http://localhost:9000   -Dsonar.login=ab438ba39290186d90891614a45727f05f3d5408"
            }
        }
        

      
        stage('Publish to Nexus') {
            steps {
                script {
                    
configFileProvider([configFile(fileId: 'maven-settings', variable: 'settings')]) {
  sh 'mvn  -B -DskipTests deploy -s $settings'}
                
                }
            }
        }
       
        

     
} 
}
       
